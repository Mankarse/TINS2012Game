#include "Engine.h"

    void Engine::logicStep() {
        
        //So where does screen control come in to this?
        
        // spawnwers gonna spawn
            // Quantity and quality of enemies is a function of total playtime
        //runSpawners(spawners, dragonStats, objectLists);
        
        // dragons gonna drag
            // Dragon spawns bullets, collides with enemies
        //runDragon(dragon, dragonStats, objectLists);
        
        // enemies move
        //runEnemies(dragonStats, objectLists);
        
        // dragon bullets fly
        //runDragonBullets(dragonBullets, enemyList);
        
        // enemy bullets fly
        //runEnemyBullets(enemyBullets, dragon);
        
        //run particle effects
        
        //Remove dead objects
        
        
        // Render steps-
            // pre-render step
            // asks each object to assign itself to a queue
            
            // render step
            // draw backgrounds
            // renders all objects in each queue in order
            // Render scores
        // flip buffers, end
        
    }